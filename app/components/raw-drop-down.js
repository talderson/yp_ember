import Component from '@ember/component';
import { inject as service } from "@ember/service";

export default class RawDropDown extends Component {
    //@service session;
    //@service currentUser;
    isAuthenticated = true;
}